export type UserMessageModelType = {
    reactUserMessage: string,
    flutterUserMessage: string,
}

export type ChatMessageModelType = {
    reactUserMessages?: string[],
    flutterUserMessages?: string[],
}